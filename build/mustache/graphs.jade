.graft
  .max {{max}}
  a.toggle(href="#") Toggle
  | {{#sets}}
  .set(class="{{color}}" data-id="{{id}}")
    .max {{max}}
    a.name(href="#") {{percent}}% {{name}}
    .intervals
      | {{#values}}
      a.interval
        .bar
      | {{/values}}
  | {{/sets}}

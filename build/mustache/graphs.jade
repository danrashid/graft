table.graft
  tr.heading
    th
      a.toggle(href="#") Toggle
    th.max {{max}}
  {{#sets}}
  tr.info(class="{{color}}" data-id="{{id}}")
    td
      a.name(href="#") {{name}}
    td.max {{max}}
  tr.set(class="{{color}}" data-id="{{id}}")
    td(colspan="2")
      table
        tr
          {{#values}}
          td.interval
            .bar &nbsp;
          {{/values}}
  {{/sets}}
